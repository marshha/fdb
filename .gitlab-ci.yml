image: node:20

variables:
  VITE_BASE_PATH: "/"

pages:
  stage: deploy
  script:
    - npm ci
    - npm run lint
    - npm run test:run
    - npm run build
  artifacts:
    paths:
      - public
  only:
    - main
